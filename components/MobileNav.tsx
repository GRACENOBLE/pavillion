import React from 'react'

const MobileNav = ({
  pathname,
  isScrolled,
  setIsMobileMenuOpen,
}: {
  pathname: string;
  isScrolled: boolean;
  setIsMobileMenuOpen : any;
}) => {
  return <div></div>;
};

export default MobileNav
